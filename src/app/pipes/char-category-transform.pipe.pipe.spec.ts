import { CharCategoryTransformPipePipe } from './char-category-transform.pipe.pipe';

describe('CharCategoryTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CharCategoryTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
