import { CharTransformPipePipe } from './char-transform.pipe.pipe';

describe('CharTransformPipePipe', () => {
  it('create an instance', () => {
    const pipe = new CharTransformPipePipe();
    expect(pipe).toBeTruthy();
  });
});
